
/**
 * Clears the sessions
 * @param {Store} store - the db store
 * @param {Logger} logger - the logger
 */
export async function sessionClear() {
  // to implement
}

/**
 * removes the un-verified users
 * @param {Store} store - the db store
 * @param {Logger} logger - the logger
 */
export async function unverifiedUserClear() {
  // to implement
}
